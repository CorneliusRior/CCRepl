<Window x:Class="ReadingList.WPF.MainWindow"
        x:Name="Root"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ReadingList.WPF"
        mc:Ignorable="d"
        Background="Black"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="White"/>
            <Setter Property="BorderThickness" Value="0.5"/>
            <Setter Property="Margin" Value="1"/>
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="15"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
        </Grid.ColumnDefinitions>

        <TextBox x:Name="tbOutput" TextWrapping="Wrap" IsReadOnly="True" AcceptsReturn="True"
                 Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2"/>
        <TextBox x:Name="tbInput" PreviewTextInput="tbInput_PreviewTextInput" KeyDown="tbInput_KeyDown" PreviewKeyDown="tbInput_PreviewKeyDown" VerticalContentAlignment="Center" Grid.Row="1" Grid.Column="0"/>
        <Button x:Name="btEnter" Click="btEnter_Click" Content="Enter" Padding="12,4" Margin="1" Background="Black" Foreground="White" Grid.Row="1" Grid.Column="1"/>

    </Grid>
</Window>
